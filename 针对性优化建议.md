# 针对性优化建议落地清单（当前版本）

本文基于“性能、功能、精准度、交互、资源”五个维度，给出**已落地改动**与**下一步建议**，便于持续迭代。

---

## 一、性能优化

## 1）已落地
- 新增总览聚合接口 `GET /api/dashboard/overview`
  - 后端并行计算（`CompletableFuture`）
  - 短缓存（默认 1 秒）降低高频刷新开销
- `NetworkFlow` 增加复合索引：
  - `(timestamp, src_ip, dst_ip)`
  - `(timestamp, protocol, app_protocol)`
- 演示数据定时检查与自动重建：
  - 默认每 5 分钟检查一次是否过期
  - 过期则自动重建并重新触发检测

## 2）下一步建议
- 采集层升级：接入 NetFlow/IPFIX 或高性能抓包（DPDK/PF_RING）
- 热冷分层：近实时写入时序库，历史降采样归档 PostgreSQL
- 预计算缓存落盘 Redis（当前为进程内缓存）

---

## 二、功能优化

## 1）已落地
- 微观流级检索增强（前后端全链路）：
  - 支持 IP/CIDR/协议/端口范围/时间范围
  - 支持分页（`page/size`）与总量返回
  - 支持 CSV 导出
- 单 IP 下钻保持可用：
  - 从 Top-K、流检索都可跳转至 IP 详情
- 全景维度切换：
  - Bytes / Packets 双维度统一切换

## 2）下一步建议
- 区域层级下钻（楼宇 -> 交换机 -> 端口）
- 区域对比与热力图
- 安全日志高级筛选、排序、导出

---

## 三、精准度优化

## 1）已落地
- 威胁检测增加慢速扫描（`SlowPortScan`）
- DDoS / PortScan / 外传阈值动态化（基于近 1 小时基线）
- 告警去重优化：
  - `威胁类型 + 源 IP + 10 分钟时间桶`

## 2）下一步建议
- 应用识别从端口规则升级到“端口 + 浅层特征”（SNI/UA）
- 增加更多威胁类型（如 ARP 欺骗、慢速外连异常）
- 增加规则参数可视化配置页面

---

## 四、交互优化

## 1）已落地
- 自动刷新稳定化：
  - 总览接口替代多接口并发轮询
  - 前端增加 in-flight 保护，避免并发重复请求导致跳变
- 图表更新策略优化：
  - 复用 ECharts 实例，避免重建
  - 时间桶对齐减少趋势图抖动
- 全站核心数值统一保留两位小数

## 2）下一步建议
- 核心指标改为 WebSocket 推送（替代轮询）
- 图表点数过多时自动降采样
- 非可视区域图表懒加载

---

## 五、资源优化

## 1）已落地
- 前端包拆分：
  - React 与 ECharts 分包，降低主包体积
- 数据生命周期：
  - 定时清理过期数据
  - 过期自动重建演示集，保持演示连续性

## 2）下一步建议
- 冷数据降采样到分钟/小时级
- 7~30 天数据归档（Parquet）
- JVM 参数与 GC 策略按压测结果细化

---

## 六、建议实施顺序（工程视角）

1. P0：采集链路与存储分层（决定上限能力）  
2. P1：WebSocket 实时推送与前端渲染优化（决定展示体验）  
3. P1：规则中心与动态阈值配置化（决定可运营性）  
4. P2：ML 异常检测与校园专属协议识别（决定竞争力）
