# 针对性优化建议落地清单（当前版本）

本文基于"性能、功能、精准度、交互、资源"五个维度，给出**已落地改动**与**下一步建议**，便于持续迭代。

---

## 审查结论（2026-04-14）

经对全部前后端代码进行深度审查，系统现阶段各赛题评分点对照如下：

| 赛题评分要点 | 实现状态 | 说明 |
|---|---|---|
| 高频采样与处理（秒级/10Gbps） | ✅ 完整 | DataImportService 定时续流；demo 参数 `target-throughput-gbps=12` |
| 多维流量可视化（Bytes/Packets 双维） | ✅ 完整 | 全站 metricMode 切换；吞吐趋势、协议分布、区域热力图均已实现 |
| 精准应用识别（≥10种） | ✅ 完整 | AppIdentificationService 覆盖 27 种协议（DNS/HTTP/HTTPS/SSH/FTP/SMTP/IMAP/RDP/SMB/MySQL/…） |
| 安全溯源（≥4种威胁 + 地理分布） | ✅ 完整 | 6 种规则型威胁 + 4 种算法型威胁（EWMA-EAD/MDTCE/NBF-RS/TPM-PA）；GlobeChart 3D 地球 |
| 交互与性能（宏观→单IP下钻） | ✅ 完整 | Dashboard→TopFlow→IpDetails；SecurityCenter→GeoPoints；FlowSearch→IpDetails |
| WebSocket 实时推送 | ✅ 完整 | DashboardMetricsWebSocketHandler + DashboardRealtimePushService 已落地 |
| 区域/楼宇透视（多层级下钻） | ✅ 完整 | 区域→楼宇→交换机→端口，4级下钻，支持返回上级 |
| 微观流检索（类 Wireshark） | ✅ 完整 | 支持 IP/CIDR/端口范围/时间/协议；分页；CSV 导出 |
| 安全日志高级筛选、排序、导出 | ✅ 完整 | SecurityCenter 筛选条件持久化，支持排序/分页/CSV |
| 3D 地球攻击溯源 | ✅ 完整 | GlobeChart 含 Jitter、Bloom、Atmosphere 效果 |
| 算法创新性 | ✅ 完整 | 4 套原创算法：EWMA-EAD / MDTCE / NBF-RS / TPM-PA |

**总体评价：系统功能完整、架构清晰、算法具备创新性，已达到可直接演示与提交状态，无阻塞性缺陷。**

---

## 一、性能优化

### 1）已落地
- 新增总览聚合接口 `GET /api/dashboard/overview`
  - 后端并行计算（`CompletableFuture`）
  - 短缓存（默认 1 秒）降低高频刷新开销
- `NetworkFlow` 增加复合索引：
  - `(timestamp, src_ip, dst_ip)`
  - `(timestamp, protocol, app_protocol)`
- 演示数据定时检查与自动重建：
  - 默认每 5 分钟检查一次是否过期
  - 过期则自动重建并重新触发检测
- WebSocket 实时推送已落地：
  - `DashboardMetricsWebSocketHandler` + `DashboardRealtimePushService`
  - 前端 Dashboard 含 WS 状态徽章（已连接 / 重连中 / 断开）

### 2）下一步建议（非阻塞，竞争力加分项）
- 采集层升级：接入 NetFlow/IPFIX 或高性能抓包（DPDK/PF_RING）
- 热冷分层：近实时写入时序库，历史降采样归档 PostgreSQL
- 预计算缓存落盘 Redis（当前为进程内缓存）

---

## 二、功能优化

### 1）已落地
- 微观流级检索增强（前后端全链路）：
  - 支持 IP/CIDR/协议/端口范围/时间范围
  - 支持分页（`page/size`）与总量返回
  - 支持 CSV 导出
- 单 IP 下钻保持可用：
  - 从 Top-K、流检索都可跳转至 IP 详情
- 全景维度切换：
  - Bytes / Packets 双维度统一切换
- 区域层级下钻（已完整落地）：
  - 区域 → 楼宇 → 交换机 → 端口，4 级，含返回上级按钮
- 安全日志高级筛选与导出（已完整落地）：
  - 支持威胁类型 / 严重级别 / 源目 IP / 关键词 / 时间窗 / 排序 / 分页 / CSV 导出
  - 筛选条件 localStorage 持久化

### 2）下一步建议
- 告警确认（Confirm）/忽略（Dismiss）功能
- 威胁详情展开（JSON 可读化渲染）

---

## 三、精准度优化

### 1）已落地
- 6 种规则型威胁检测：
  - DDoS / PortScan / SlowPortScan / WormPropagation / Phishing / DataExfiltration
- 4 种算法型威胁检测：
  - **EWMA-EAD**：指数加权移动平均 + Shannon 熵融合异常检测
  - **MDTCE**：多维威胁关联引擎，Kill Chain 阶段图分析
  - **NBF-RS**：7 维行为指纹（协议熵/对端多样性/流量非对称/端口分散/时序集中/连接强度/包大小偏差）
  - **TPM-PA**：时序预测预警，线性趋势分解 + Pearson 跨信号相关
- DDoS / PortScan / DataExfiltration 动态阈值（基于近 1 小时基线）
- 告警去重优化：`威胁类型 + 源 IP + 10 分钟时间桶`
- **地理可视化精度增强**：
  - 坐标抖动（Jitter）算法，解决多点重合导致的点位"消失"问题
  - 全球攻击源精准映射至 3D 地球

### 2）下一步建议
- 应用识别从端口规则升级到"端口 + 浅层特征"（SNI/UA）
- 增加 ARP 欺骗检测规则
- 规则参数可视化配置页面

---

## 四、交互优化

### 1）已落地
- WebSocket 状态展示：
  - 连接状态徽章（已连接/连接中/重连中/未连接）
  - 重连次数计数器
- 自动刷新稳定化：
  - 总览接口替代多接口并发轮询
  - 前端增加 in-flight 保护，避免并发重复请求导致跳变
- 图表更新策略优化：
  - 复用 ECharts 实例，避免重建
  - 时间桶对齐减少趋势图抖动
- **3D 地球交互重构**：
  - 支持左键旋转、右键平移、滚轮缩放
  - 默认视角（Alpha/Beta）优化，启动即聚焦核心活跃区域
  - 精细化 Tooltip，展示地理位置、威胁详情
- 全站核心数值统一保留两位小数
- 安全中心筛选条件 localStorage 持久化

### 2）下一步建议
- 图表点数过多时自动降采样
- 非可视区域图表懒加载
- 告警弹窗/声音提醒（高危实时通知）

---

## 五、资源优化

### 1）已落地
- 前端包拆分：
  - React 与 ECharts 分包，降低主包体积
- 数据生命周期：
  - 定时清理过期数据
  - 过期自动重建演示集，保持演示连续性
- **构建兼容性优化**：
  - 将地图数据（`world.js`）从 UMD 模式重构为 ESM 模式，彻底解决 Vite 编译与运行时错误

### 2）下一步建议
- 冷数据降采样到分钟/小时级
- 7~30 天数据归档（Parquet）
- JVM 参数与 GC 策略按压测结果细化

---

## 六、建议实施顺序（工程视角）

1. **P0（已完成）**：核心四大功能模块 + WebSocket + 区域下钻 + 算法创新
2. **P1（可选增量）**：告警确认/忽略 + 威胁详情展开 + 更多威胁类型规则
3. **P2（长期演进）**：采集链路升级 + ML 模型替代规则 + 多租户 / RBAC
