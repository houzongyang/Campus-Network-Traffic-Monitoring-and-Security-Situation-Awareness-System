# 技术与算法说明

## 1. 技术栈

### 后端
- `Spring Boot 3.x`（REST API）
- `Spring Data JPA / Hibernate`（数据访问）
- `PostgreSQL`（流量与告警存储）
- `springdoc-openapi`（Swagger 接口文档）
- `@Scheduled`（演示数据续流、清理、过期重建）

### 前端
- `React 18 + Vite 5`
- `Zustand`（状态管理）
- `Axios`（接口请求）
- `ECharts`（趋势、分布、散点可视化）

### 部署
- `Docker / Docker Compose`
- `Nginx`（前端容器静态服务与反向代理）

---

## 2. 主要数据模型

### `NetworkFlow`
核心字段：
- 网络五元组：`srcIp`、`dstIp`、`srcPort`、`dstPort`、`protocol`
- 流量计量：`bytesSent/bytesRecv`、`packetsSent/packetsRecv`
- 识别与标签：`appProtocol`、`region`、`direction`
- 时间字段：`startTime`、`endTime`、`timestamp`

索引策略（已落地）：
- 单列索引：`timestamp`、`src_ip`、`dst_ip`、`app_protocol`、`region`
- 复合索引：`(timestamp, src_ip, dst_ip)`、`(timestamp, protocol, app_protocol)`

### `SecurityAlert`
核心字段：
- `alertType`、`severity`
- `srcIp`、`dstIp`
- `detectedTime`
- `description`
- `threatDetails`（JSON）
- 地理字段：`country`、`city`、`latitude`、`longitude`

---

## 3. 核心算法与实现

## 3.1 全景指标计算（FlowAnalysisService）
- 吞吐量（Mbps）：
  - `sum(bytesSent + bytesRecv) * 8 / durationSeconds / 1_000_000`
- PPS：
  - `sum(packetsSent + packetsRecv) / durationSeconds`
- 活跃 IP：
  - 时间窗内 `srcIp + dstIp` 去重计数
- 协议分布：
  - 按 `appProtocol` 分组并按 `bytes` 或 `packets` 聚合
- Top-K 大流：
  - 按 `bytes` 或 `packets` 排序，输出前 K 条
- 区域流量：
  - 按 `region` 聚合 `bytes/packets/flowCount/activeIps`
- 吞吐趋势：
  - 固定时间桶（`bucketMinutes`）聚合，时间对齐减少图表跳变

## 3.2 总览并行计算与缓存（DashboardOverviewService）
- 新增统一接口：`GET /api/dashboard/overview`
- 使用 `CompletableFuture` 并行计算：
  - 指标、Top-K、区域统计、趋势
- 增加短缓存（默认 `1s`）：
  - 降低高频自动刷新时的后端重复计算

## 3.3 流级检索与导出（FlowSearchController + FlowAnalysisService）
- 检索接口：`POST /api/flows/search`
- 导出接口：`GET /api/flows/search/export`
- 支持条件：
  - `srcIp/dstIp`（精确或 `*` 通配）
  - `srcCidr/dstCidr`（CIDR 匹配）
  - `srcPort/dstPort`、`dstPortRange`
  - `protocol`、`appProtocol`
  - `startTime/endTime` 或 `minutesAgo`
  - `page/size` 分页
- 导出格式：CSV

## 3.4 威胁检测规则（ThreatDetectionService）
当前规则类型：
- `DDoS`
- `PortScan`
- `SlowPortScan`
- `WormPropagation`
- `Phishing`
- `DataExfiltration`

精度优化（已落地）：
- 动态阈值：
  - DDoS、端口扫描、外传阈值基于近 1 小时基线计算
- 慢速扫描检测：
  - 长时间窗口内扫描大量端口但速率低
- 告警去重：
  - `alertType + srcIp + 10分钟时间桶`

## 3.5 演示数据生成与自动维护（DataImportService）
- 启动自检：无数据或数据过期自动重建
- 定时续流：持续生成基线流量与小比例异常流量
- 定时清理：清理超出保留时间的数据
- 定时过期检查：每 3~5 分钟级别可配置自动检查并重建

---

## 4. 前端交互与可视化策略
- 图表实例复用：避免每次刷新重建 ECharts 实例
- 自动刷新稳定化：
  - 合并为总览接口请求
  - 防止并发重复请求（in-flight 保护）
- 数值统一显示：核心数据统一保留两位小数
- 下钻路径：
  - 全景 Top-K 流量 -> 单 IP 详情
- 流检索增强：
  - 高级条件 + 分页 + CSV 导出

---

## 5. 关键接口清单（新增/增强）
- `GET /api/dashboard/overview`
- `POST /api/flows/search`（高级检索 + 分页）
- `GET /api/flows/search/export`（CSV 导出）
- `GET /api/flows/ip-profile/{ip}`（单 IP 画像）
