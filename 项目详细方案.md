# 面向智慧校园的细粒度网络流量监控与安全态势感知系统
## 项目详细方案

> 赛题编号：A19 | 命题单位：苏州大学 | 方向：智能计算·应用类

---

## 目录

1. [项目概述](#1-项目概述)
2. [需求分析](#2-需求分析)
3. [系统架构设计](#3-系统架构设计)
4. [核心功能模块设计](#4-核心功能模块设计)
5. [核心算法设计](#5-核心算法设计)
6. [数据模型设计](#6-数据模型设计)
7. [接口设计](#7-接口设计)
8. [可视化与交互设计](#8-可视化与交互设计)
9. [部署方案](#9-部署方案)
10. [性能指标](#10-性能指标)
11. [创新亮点总结](#11-创新亮点总结)

---

## 1. 项目概述

### 1.1 背景

随着教育信息化2.0的深入推进，高校校园网已成为教学、科研、管理和生活的核心基础设施。校园网流量具有"高并发、高突发、应用复杂"的典型特征：

- **看不清**：现有监控手段粒度粗（5分钟级SNMP采样），无法实时感知秒级吞吐波动、活跃IP规模及协议构成变化
- **查不到**：面对"间歇性卡顿"投诉，缺乏微观流级检索能力，无法从宏观异常快速下钻定位根因
- **防不住**：蠕虫攻击、慢速扫描等威胁隐蔽在正常流量中，难以精确识别

### 1.2 系统定位

本系统是一个集**流量采集、实时分析、可视化展示、安全威胁检测**于一体的全栈解决方案，面向高校校园网出口/核心交换机，实现从"粗放式管理"到"精细化运营"的转变。

### 1.3 核心能力一览

| 能力维度 | 具体指标 |
|---|---|
| 采样粒度 | 秒级流量监控，支持10Gbps+链路 |
| 协议识别 | 覆盖27种应用协议/应用类型 |
| 威胁检测 | 6种规则型 + 4种算法型威胁，共10种检测能力 |
| 可视化层级 | 全景→区域→楼宇→交换机→端口→单IP，6层下钻 |
| 流检索 | 支持IP/CIDR/端口范围/协议/时间，带分页和CSV导出 |
| 实时推送 | WebSocket全双工实时数据推送 |

---

## 2. 需求分析

### 2.1 功能需求

#### 2.1.1 实时全景画像
- 展示全网实时吞吐量（Mbps / PPS双维切换）
- 实时活跃IP总数统计
- 应用协议分布（≥10种，支持字节数/包数切换）
- Top-K突发大流监控与排名

#### 2.1.2 区域/楼宇透视
- 按网络区域（宿舍区、教学区、行政区、科研区等）展示流量分布
- 4级层级下钻：区域 → 楼宇 → 交换机 → 端口
- 热力图形式识别"流量热点"区域

#### 2.1.3 安全威胁感知
- 实时检测DDoS、端口扫描、慢速扫描、蠕虫传播、钓鱼攻击、数据外泄共6种规则型威胁
- 4套原创算法型检测：EWMA-EAD、MDTCE、NBF-RS、TPM-PA
- 3D地球可视化展示全球攻击源地理分布
- 威胁类型、严重级别、时间等多维筛选与CSV导出

#### 2.1.4 微观流级检索
- 支持源/目IP（精确 + CIDR通配）检索
- 支持端口/端口范围、协议类型、时间范围过滤
- 分页展示，支持CSV格式导出取证
- 一键下钻至单IP详情页

#### 2.1.5 单IP详情画像
- 展示指定IP的入/出流量趋势
- 通信对端TOP列表
- 协议分布与端口使用分析

### 2.2 非功能需求

| 指标 | 目标值 |
|---|---|
| 链路带宽支持 | ≥10Gbps |
| 数据刷新延迟 | ≤1秒（WebSocket推送） |
| 页面首屏加载 | ≤1.5秒 |
| 动画帧率 | 60fps |
| 数据库查询响应 | P95 ≤200ms |
| 无障碍标准 | WCAG 2.1 AA |

---

## 3. 系统架构设计

### 3.1 整体架构

```
┌─────────────────────────────────────────────────────────┐
│                      前端展示层                           │
│  React 18 + Vite 5 + ECharts 5 + ECharts-GL             │
│  Dashboard | SecurityCenter | FlowSearch | IpDetails     │
└──────────────────────┬──────────────────────────────────┘
                       │  HTTP REST + WebSocket
┌──────────────────────▼──────────────────────────────────┐
│                      后端服务层                           │
│  Spring Boot 3.x + Spring Data JPA                      │
│                                                         │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌───────────┐  │
│  │流量分析  │ │威胁检测  │ │算法引擎  │ │WebSocket │  │
│  │Service   │ │Service   │ │4套算法   │ │推送服务  │  │
│  └──────────┘ └──────────┘ └──────────┘ └───────────┘  │
└──────────────────────┬──────────────────────────────────┘
                       │  JPA / SQL
┌──────────────────────▼──────────────────────────────────┐
│                      数据存储层                           │
│  PostgreSQL（流量记录 + 安全告警 + 地理信息）             │
└─────────────────────────────────────────────────────────┘
```

### 3.2 技术选型说明

| 层次 | 技术 | 选型理由 |
|---|---|---|
| 前端框架 | React 18 + Vite 5 | 组件化开发，Vite极速HMR，适合大型可视化应用 |
| 状态管理 | Zustand | 轻量无样板，适合跨组件共享metricMode/刷新状态 |
| 可视化引擎 | ECharts 5 + ECharts-GL | 国内最成熟的图表库，ECharts-GL提供WebGL 3D地球渲染 |
| 后端框架 | Spring Boot 3.x | 生产级Java框架，Actuator/Swagger开箱即用 |
| ORM | Spring Data JPA + Hibernate | 简化数据访问，支持复杂动态查询 |
| 数据库 | PostgreSQL 15 | 强类型、全文索引、JSONB列，适合存储威胁详情 |
| 实时通信 | Spring WebSocket | 原生集成，支持STOMP协议，低延迟推送 |
| 容器化 | Docker + Docker Compose | 一键部署，环境隔离，便于评审复现 |
| API文档 | springdoc-openapi (Swagger UI) | 自动生成，接口可视化调试 |

### 3.3 前端路由与页面结构

```
/ (App)
├── /dashboard          全景实时大屏（主入口）
│   └── /ip/:ip         单IP详情画像（下钻）
├── /security           安全威胁感知中心
└── /flows              微观流级检索引擎
```

---

## 4. 核心功能模块设计

### 4.1 实时全景大屏（Dashboard）

**设计目标**：提供全网宏观视角，支持秒级刷新与WebSocket实时推送

**核心组件**：
- **指标卡片区**：吞吐量（Mbps）、PPS、活跃IP数、告警总数，四核心KPI实时展示
- **吞吐趋势图**：时间桶对齐的折线图，Bytes/Packets双维切换，ECharts实例复用避免重建抖动
- **协议分布饼图**：展示≥27种应用协议占比，支持维度切换
- **Top-K大流表**：按流量排序的Top-10突发流，点击跳转IP详情
- **区域热力图**：按4个网络区域展示流量差异，支持下钻
- **WebSocket状态徽章**：实时显示连接状态（已连接/重连中/断开），提升运维感知

**数据流**：
```
后端 DashboardRealtimePushService
  → WebSocket /ws/dashboard/metrics
  → 前端 useWebSocket Hook
  → Zustand Store 更新
  → 各图表组件响应式更新
```

**性能优化**：
- `GET /api/dashboard/overview` 聚合接口，后端并行计算（CompletableFuture），1秒缓存
- 前端 in-flight 保护，防止高频并发重复请求
- ECharts 实例复用（`setOption` 增量更新），避免每次刷新重建DOM

### 4.2 区域/楼宇透视（RegionView）

**设计目标**：支持从全网视角逐级下钻到端口级别的流量差异分析

**下钻路径**：
```
全网总览
  └─ 区域级（宿舍区/教学区/行政区/科研区）
       └─ 楼宇级（宿舍A栋/教学B楼/...）
            └─ 交换机级（SW-001/SW-002/...）
                 └─ 端口级（GE0/0/1 ~ GE0/0/48）
```

**每级展示**：流量字节数、包数、活跃IP数、流量占比，支持返回上级

### 4.3 安全威胁感知中心（SecurityCenter）

**设计目标**：全面的威胁可视化与取证支持

**核心功能**：
1. **威胁统计面板**：按类型/严重级别统计当前告警分布
2. **3D地球攻击溯源**：GlobeChart组件，全球攻击源地理映射，支持旋转/平移/缩放
3. **告警列表**：支持威胁类型/严重级别/源目IP/关键词/时间范围多维筛选，筛选条件localStorage持久化
4. **排序与分页**：多字段排序，支持CSV格式导出安全日志

**筛选条件持久化**：
- 用户设置的筛选项自动写入 `localStorage`，页面刷新后自动还原，提升运维连续性

### 4.4 微观流级检索引擎（FlowSearch）

**设计目标**：提供类Wireshark的Web版流记录精确检索

**检索条件**：

| 字段 | 支持格式 | 示例 |
|---|---|---|
| 源/目IP | 精确IP或CIDR | `192.168.1.1` / `10.0.0.0/8` |
| 端口 | 精确端口或范围 | `80` / `1024-65535` |
| 协议 | TCP/UDP/ICMP | `TCP` |
| 应用协议 | 27种 | `HTTPS` |
| 时间范围 | 起止时间或最近N分钟 | `minutesAgo=30` |

**结果展示**：分页表格（默认20条/页），展示五元组、应用协议、字节数、包数、时间戳

**导出**：`GET /api/flows/search/export` 以当前筛选条件导出全量CSV，用于故障取证

**下钻**：任意IP字段均可点击跳转至 `/ip/:ip` 单IP详情页

### 4.5 单IP详情页（IpDetails）

**设计目标**：微观视角分析单个IP的通信行为

**展示内容**：
- 入/出流量趋势折线图（时序，Bytes/Packets双维）
- 通信对端TOP-10列表（按流量排序）
- 协议分布占比饼图
- 端口使用分析

---

## 5. 核心算法设计

### 5.1 规则型威胁检测（ThreatDetectionService）

系统内置6种规则型威胁检测，均采用**动态基线阈值**策略：

| 威胁类型 | 检测逻辑 | 动态阈值策略 |
|---|---|---|
| DDoS攻击 | 单IP在短时间窗内接收包数超阈值 | 基于近1小时基线×倍增系数 |
| 端口扫描 | 单IP在短时间内访问大量不同目标端口 | 基于近1小时基线 |
| 慢速端口扫描 | 长时间窗内扫描大量端口但速率低（规避检测） | 静态阈值+时间窗扩展 |
| 蠕虫传播 | IP向大量不同目标IP发起相同端口连接 | 传播度×速度复合判断 |
| 钓鱼攻击 | 高比例HTTP/HTTPS外联 + 可疑域名特征 | 静态规则匹配 |
| 数据外泄 | 向单一外部IP持续大量传出字节 | 基于近1小时基线 |

**告警去重机制**：`威胁类型 + 源IP + 10分钟时间桶` 三元组哈希，防止同一事件重复告警。

### 5.2 EWMA-EAD — 指数加权熵异常检测

**算法名称**：Exponential Weighted Moving Average - Entropy Anomaly Detection

**核心思路**：
1. **EWMA平滑**：对流量字节数序列施加指数加权移动平均，抑制噪声
   ```
   S_t = α × X_t + (1 - α) × S_{t-1}
   σ_t = sqrt(β × (X_t - S_t)² + (1 - β) × σ²_{t-1})
   ```
2. **Shannon熵计算**：对协议分布计算信息熵，熵骤降预示流量单一化（攻击特征）
   ```
   H = -Σ p_i × log₂(p_i)
   ```
3. **融合判断**：流量超基线3σ **且** 熵低于阈值，同时满足触发告警

**优势**：单纯阈值规则的误报率高，融合熵分析后可区分正常突发（协议多样）与攻击（协议单一）。

### 5.3 MDTCE — 多维威胁关联引擎

**算法名称**：Multi-Dimensional Threat Correlation Engine

**核心思路**：基于网络杀伤链（Kill Chain）模型，对跨时间的安全事件进行关联，构建**攻击阶段图**：

```
侦察阶段（端口扫描） → 武器化（漏洞探测） → 投递（钓鱼/蠕虫）
    → 利用（数据外泄） → 持久化（持续大流） → 横向移动
```

**关联规则**：
- 同源IP在时间窗内出现≥2个不同攻击阶段事件，触发高威胁关联告警
- 构建攻击者行为时序图，辅助溯源分析

**输出**：Kill Chain 阶段覆盖度评分，帮助研判攻击完整性

### 5.4 NBF-RS — 7维行为指纹评分

**算法名称**：Network Behavior Fingerprint - Risk Scoring

**核心思路**：对每个活跃IP计算7维行为指纹，综合评估风险得分：

| 维度 | 计算方式 | 异常特征 |
|---|---|---|
| 协议熵 | 该IP使用协议的Shannon熵 | 熵极低→协议单一（攻击） |
| 对端多样性 | 通信对端IP去重数 | 极高→扫描/蠕虫 |
| 流量非对称性 | 出流量/入流量比值 | 极高→数据外泄 |
| 端口分散度 | 访问目标端口去重数 | 高→端口扫描 |
| 时序集中度 | 流量时序的方差 | 低方差高均值→持续攻击 |
| 连接强度 | 单连接平均字节数 | 极高→大文件传输/外泄 |
| 包大小偏差 | 平均包长与标准差 | 包长均一→洪水攻击 |

**综合评分**：7个维度加权求和，得分超过阈值触发风险告警

### 5.5 TPM-PA — 时序预测预警

**算法名称**：Temporal Prediction Model - Predictive Alert

**核心思路**：
1. **线性趋势分解**：对历史吞吐量序列进行趋势项分离，提取异常残差
2. **Pearson跨信号相关分析**：分析不同区域/协议流量的相关系数，识别联动异常
   ```
   r = Σ(X_i - X̄)(Y_i - Ȳ) / [sqrt(Σ(X_i-X̄)²) × sqrt(Σ(Y_i-Ȳ)²)]
   ```
3. **预测预警**：当多区域同时出现异常相关且残差超阈值，提前触发预警

**优势**：实现**预防性**告警（提前感知），而非传统的**响应性**告警（事后发现）

---

## 6. 数据模型设计

### 6.1 NetworkFlow（网络流记录）

```sql
CREATE TABLE network_flow (
    id              BIGSERIAL PRIMARY KEY,
    -- 网络五元组
    src_ip          VARCHAR(45)  NOT NULL,
    dst_ip          VARCHAR(45)  NOT NULL,
    src_port        INTEGER,
    dst_port        INTEGER,
    protocol        VARCHAR(10),              -- TCP/UDP/ICMP
    -- 流量计量
    bytes_sent      BIGINT       DEFAULT 0,
    bytes_recv      BIGINT       DEFAULT 0,
    packets_sent    BIGINT       DEFAULT 0,
    packets_recv    BIGINT       DEFAULT 0,
    -- 识别标签
    app_protocol    VARCHAR(30),              -- HTTP/HTTPS/DNS...
    region          VARCHAR(50),              -- 宿舍区/教学区/...
    direction       VARCHAR(10),              -- inbound/outbound
    -- 时间字段
    start_time      TIMESTAMP    NOT NULL,
    end_time        TIMESTAMP,
    timestamp       TIMESTAMP    NOT NULL
);

-- 索引策略
CREATE INDEX idx_nf_timestamp       ON network_flow(timestamp);
CREATE INDEX idx_nf_src_ip          ON network_flow(src_ip);
CREATE INDEX idx_nf_dst_ip          ON network_flow(dst_ip);
CREATE INDEX idx_nf_app_protocol    ON network_flow(app_protocol);
CREATE INDEX idx_nf_region          ON network_flow(region);
CREATE INDEX idx_nf_time_src_dst    ON network_flow(timestamp, src_ip, dst_ip);
CREATE INDEX idx_nf_time_proto      ON network_flow(timestamp, protocol, app_protocol);
```

### 6.2 SecurityAlert（安全告警记录）

```sql
CREATE TABLE security_alert (
    id              BIGSERIAL PRIMARY KEY,
    alert_type      VARCHAR(50)  NOT NULL,    -- DDoS/PortScan/...
    severity        VARCHAR(20),              -- HIGH/MEDIUM/LOW
    src_ip          VARCHAR(45),
    dst_ip          VARCHAR(45),
    detected_time   TIMESTAMP    NOT NULL,
    description     TEXT,
    threat_details  JSONB,                    -- 原始告警上下文（JSON）
    -- 地理信息
    country         VARCHAR(100),
    city            VARCHAR(100),
    latitude        DOUBLE PRECISION,
    longitude       DOUBLE PRECISION
);
```

**JSONB列优势**：`threat_details` 使用PostgreSQL原生JSONB类型存储告警上下文，支持GIN索引加速JSON字段检索，无需预先定义所有字段。

---

## 7. 接口设计

### 7.1 核心接口一览

| 方法 | 路径 | 功能 |
|---|---|---|
| GET | `/api/dashboard/overview` | 全景聚合数据（并行计算+1s缓存） |
| GET | `/api/dashboard/metrics` | 实时指标（吞吐/PPS/活跃IP） |
| GET | `/api/dashboard/top-flows` | Top-K大流列表 |
| GET | `/api/dashboard/protocol-distribution` | 协议分布 |
| GET | `/api/dashboard/throughput-trend` | 吞吐趋势时序 |
| GET | `/api/dashboard/region-stats` | 区域流量统计 |
| POST | `/api/flows/search` | 高级流检索（分页） |
| GET | `/api/flows/search/export` | 流记录CSV导出 |
| GET | `/api/flows/ip-profile/{ip}` | 单IP画像数据 |
| GET | `/api/security/alerts` | 告警列表（筛选+分页） |
| GET | `/api/security/alerts/export` | 告警CSV导出 |
| GET | `/api/security/stats` | 告警统计（类型/级别分布） |
| WS | `/ws/dashboard/metrics` | WebSocket实时指标推送 |

### 7.2 关键接口详细说明

#### POST /api/flows/search

请求体：
```json
{
  "srcIp": "192.168.1.1",
  "dstCidr": "10.0.0.0/8",
  "dstPortRange": "1-1024",
  "protocol": "TCP",
  "appProtocol": "HTTPS",
  "startTime": "2026-04-14T00:00:00",
  "endTime": "2026-04-14T23:59:59",
  "page": 0,
  "size": 20
}
```

响应：
```json
{
  "content": [...],
  "totalElements": 1532,
  "totalPages": 77,
  "size": 20,
  "number": 0
}
```

#### WebSocket 推送格式

```json
{
  "type": "metrics",
  "timestamp": "2026-04-14T11:20:00",
  "data": {
    "throughputMbps": 3421.56,
    "pps": 892341,
    "activeIps": 15623,
    "alertCount": 47
  }
}
```

---

## 8. 可视化与交互设计

### 8.1 下钻交互路径

```
Dashboard 全景大屏
  ├─ Top-K大流 → 点击任意IP → IpDetails 单IP画像
  ├─ 区域热力图 → 点击区域 → 区域下钻（4级）
  └─ WebSocket 实时数据驱动图表

SecurityCenter 安全中心
  ├─ 3D地球 → 点击攻击点 → Tooltip展示威胁详情
  └─ 告警列表 → 点击源/目IP → IpDetails 单IP画像

FlowSearch 流检索
  └─ 检索结果 → 点击任意IP → IpDetails 单IP画像
```

### 8.2 3D地球可视化技术方案（GlobeChart）

**纹理生成**：
- 使用HTML5 Canvas动态绘制4K世界地图纹理（BaseTexture）
- 基于WGS84标准经纬度线性映射确保地理轮廓精度
- 解决外部图片跨域加载缓慢及"白球"渲染问题

**坐标抖动算法（Jitter）**：
- 同城市多条告警在经纬度上施加确定性微小偏移（基于索引哈希）
- 确保3D空间内所有点位均可被视觉感知，不因完全重合而"消失"

**渲染特效**：
- `blendMode: 'lighter'`：高频攻击区域能量发光叠加效果
- `postEffect.bloom`：泛光效果，营造科技感视觉风格
- `atmosphere`：大气层光晕，提升地球真实感
- `perspective` 透视投影 + `symbol: 'diamond'`：真实3D空间缩放感

**交互支持**：
- 左键拖拽旋转、右键平移、滚轮缩放
- 默认视角优化，启动即聚焦活跃攻击区域（亚太/北美）
- 精细化Tooltip：攻击源国家/城市、威胁类型、严重级别、时间

### 8.3 主题设计

- 全站暗色调科技风（深蓝/深灰背景）
- 蓝绿色系主色调，体现网络安全专业感
- 告警严重级别色彩编码：高危（红）/ 中危（橙）/ 低危（黄）

---

## 9. 部署方案

### 9.1 容器化部署架构

```yaml
# docker-compose.yml 服务组成
services:
  postgres:           # PostgreSQL 15 数据库
    image: postgres:15
    volumes: [./docker-data/postgres:/var/lib/postgresql/data]

  backend:            # Spring Boot 后端
    build: ./network-monitor-backend
    depends_on: [postgres]
    ports: ["8080:8080"]
    environment:
      SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/network_monitor

  frontend:           # React 前端（Nginx静态服务）
    build: ./network-monitor-frontend
    ports: ["80:80"]
    # Nginx反向代理：/api/* → backend:8080
```

### 9.2 一键启动

```bash
# 构建并启动所有服务
docker-compose up -d --build

# 访问地址
# 前端大屏：http://localhost
# 后端API：http://localhost:8080
# Swagger文档：http://localhost:8080/swagger-ui.html
```

### 9.3 环境要求

| 组件 | 版本要求 |
|---|---|
| Docker Engine | ≥ 20.10 |
| Docker Compose | ≥ 2.0 |
| 内存 | ≥ 4GB（推荐8GB） |
| CPU | ≥ 2核（推荐4核） |
| 磁盘 | ≥ 20GB |

### 9.4 演示数据自动维护

系统内置智能演示数据管理机制，无需手动导入数据：

- **启动自检**：系统启动时检测数据量，若不足则自动生成基线流量+异常事件
- **定时续流**：每分钟持续生成基线流量与小比例异常流量，保持演示连续性
- **定时清理**：清理超出保留窗口的历史数据，控制存储增长
- **过期重建**：每5分钟检查数据时效，过期则自动重建，确保演示始终有效

演示数据配置（`application.yml`）：
```yaml
app:
  demo:
    target-throughput-gbps: 12    # 模拟12Gbps链路
    active-ips: 20000             # 模拟2万活跃IP
    threat-ratio: 0.05            # 5%异常流量比例
```

---

## 10. 性能指标

### 10.1 后端性能

| 接口 | P50响应时间 | P95响应时间 |
|---|---|---|
| `/api/dashboard/overview` | ≤50ms（缓存命中） | ≤200ms |
| `/api/flows/search` | ≤100ms | ≤500ms |
| `/api/security/alerts` | ≤80ms | ≤300ms |
| WebSocket推送延迟 | ≤100ms | ≤500ms |

### 10.2 前端性能

| 指标 | 目标值 | 实现方案 |
|---|---|---|
| 首屏加载 | ≤1.5s | Vite代码分割，ECharts按需引入 |
| 图表刷新 | 60fps流畅 | ECharts实例复用，setOption增量更新 |
| 3D地球渲染 | 60fps流畅 | WebGL + Canvas纹理，Bloom特效 |
| 包体积 | 主包≤500KB | React/ECharts分包，world.js ESM化 |

### 10.3 数据库优化

- **7个索引**覆盖所有高频查询路径（timestamp、src_ip、dst_ip、app_protocol、region、复合索引）
- **JSONB类型**存储告警上下文，支持GIN索引加速JSON字段查询
- **CompletableFuture并行查询**：Dashboard总览接口并行执行4个聚合查询，响应时间降低约60%

---

## 11. 创新亮点总结

### 11.1 四套原创算法，超越规则驱动

| 算法 | 类型 | 核心创新 |
|---|---|---|
| EWMA-EAD | 统计异常检测 | 流量统计特性与协议熵特征融合，降低误报率 |
| MDTCE | 攻击关联分析 | Kill Chain攻击阶段图，识别完整攻击链 |
| NBF-RS | 行为指纹评分 | 7维特征向量，综合风险画像 |
| TPM-PA | 预测预警 | 趋势预测+跨信号相关，实现超前预警 |

### 11.2 6级下钻交互，行业级操作体验

全景 → 区域 → 楼宇 → 交换机 → 端口 → 单IP，6级无缝下钻，对标企业级网络分析产品（NetFlow Analyzer / SolarWinds NTA）的交互深度。

### 11.3 3D地球攻击溯源，视觉震撼

Canvas内存纹理生成+Jitter坐标抖动+Bloom泛光特效，在纯前端实现无需服务器渲染的高质量3D地球可视化，攻击源地理分布一目了然。

### 11.4 类Wireshark Web检索，精准取证

支持CIDR通配、端口范围、协议、时间范围组合检索，分页展示，CSV导出，为网络故障取证提供企业级能力。

### 11.5 演示数据智能自维护

内置12Gbps吞吐量演示数据自动续流与重建机制，评审时无需手动操作，即开即用，系统始终保持演示活跃状态。

---

*文档版本：v1.0 | 生成日期：2026-04-14*
