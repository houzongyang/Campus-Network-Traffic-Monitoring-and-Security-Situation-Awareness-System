# 校园网络监控系统 - 服务器部署文档

## 系统架构概览

本系统采用前后端分离架构，包含以下组件：

| 组件 | 技术栈 | 端口 | 说明 |
|------|--------|------|------|
| 前端 | React + Vite + Nginx | 3000 | 数据可视化大屏 |
| 后端 | Spring Boot 3.2 + Java 17 | 8080 | REST API + WebSocket |
| 数据库 | PostgreSQL 15 | 5432 | 数据持久化 |

## 一、服务器环境要求

### 1.1 硬件要求

| 资源 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 2核 | 4核及以上 |
| 内存 | 4GB | 8GB及以上 |
| 磁盘 | 20GB | 50GB SSD及以上 |
| 网络 | 1Mbps | 5Mbps及以上 |

### 1.2 软件要求

- **操作系统**: Ubuntu 20.04/22.04 LTS 或 CentOS 7/8
- **Docker**: 20.10.0 及以上版本
- **Docker Compose**: 2.0.0 及以上版本
- **Git**: 用于拉取代码

## 二、服务器准备工作

### 2.1 安装 Docker 和 Docker Compose

**Ubuntu/Debian 系统：**

```bash
# 更新系统包
sudo apt-get update
sudo apt-get upgrade -y

# 安装必要依赖
sudo apt-get install -y apt-transport-https ca-certificates curl gnupg lsb-release git

# 添加 Docker 官方 GPG 密钥
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo gpg --dearmor -o /usr/share/keyrings/docker-archive-keyring.gpg

# 添加 Docker 软件源
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/docker-archive-keyring.gpg] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable" | sudo tee /etc/apt/sources.list.d/docker.list > /dev/null

# 安装 Docker
sudo apt-get update
sudo apt-get install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 启动 Docker 并设置开机自启
sudo systemctl start docker
sudo systemctl enable docker

# 验证安装
docker --version
docker compose version
```

**CentOS/RHEL 系统：**

```bash
# 安装必要依赖
sudo yum install -y yum-utils device-mapper-persistent-data lvm2 git

# 添加 Docker 软件源
sudo yum-config-manager --add-repo https://download.docker.com/linux/centos/docker-ce.repo

# 安装 Docker
sudo yum install -y docker-ce docker-ce-cli containerd.io docker-compose-plugin

# 启动 Docker 并设置开机自启
sudo systemctl start docker
sudo systemctl enable docker

# 验证安装
docker --version
docker compose version
```

### 2.2 配置 Docker 用户权限（可选）

```bash
# 将当前用户加入 docker 组，免 sudo 使用 docker
sudo usermod -aG docker $USER

# 重新登录或执行以下命令使配置生效
newgrp docker
```

### 2.3 配置防火墙

```bash
# Ubuntu 使用 UFW
sudo ufw allow 22/tcp      # SSH
sudo ufw allow 80/tcp      # HTTP
sudo ufw allow 443/tcp     # HTTPS
sudo ufw allow 3000/tcp    # 前端服务
sudo ufw allow 8080/tcp    # 后端 API（如需直接访问）
sudo ufw enable

# CentOS 使用 Firewalld
sudo firewall-cmd --permanent --add-port=22/tcp
sudo firewall-cmd --permanent --add-port=80/tcp
sudo firewall-cmd --permanent --add-port=443/tcp
sudo firewall-cmd --permanent --add-port=3000/tcp
sudo firewall-cmd --permanent --add-port=8080/tcp
sudo firewall-cmd --reload
```

## 三、部署步骤

### 3.1 创建部署目录

```bash
# 创建项目目录
mkdir -p /opt/network-monitor
cd /opt/network-monitor
```

### 3.2 上传项目代码

**方式一：通过 Git 克隆（推荐）**

```bash
# 克隆代码仓库
git clone <你的代码仓库地址> .

# 或如果是私有仓库
git clone https://username:password@github.com/username/repo.git .
```

**方式二：通过 SCP/FTP 上传**

```bash
# 在本地执行，将代码上传到服务器
scp -r ./network-monitor-backend ./network-monitor-frontend ./docker-compose.yml user@server_ip:/opt/network-monitor/
```

### 3.3 配置环境变量（可选）

创建环境变量文件：

```bash
cd /opt/network-monitor
nano .env
```

添加以下内容（根据实际需求修改）：

```env
# 数据库配置
POSTGRES_HOST_PORT=55432

# 应用配置
APP_DEMO_AUTO_INIT=false
APP_IMPORT_HOST_PATH=/opt/network-monitor/imports

# 可选：性能调优参数
APP_DASHBOARD_OVERVIEW_CACHE_MS=1000
APP_DASHBOARD_WS_PUSH_INTERVAL_MS=2000
```

### 3.4 构建并启动服务

```bash
cd /opt/network-monitor

# 构建镜像并启动服务
docker compose up -d --build

# 查看构建和启动日志
docker compose logs -f
```

首次构建可能需要 5-15 分钟，取决于网络速度和服务器性能。

### 3.5 验证部署状态

```bash
# 查看所有容器状态
docker compose ps

# 查看后端服务日志
docker compose logs -f backend

# 查看前端服务日志
docker compose logs -f frontend

# 查看数据库日志
docker compose logs -f postgres
```

### 3.6 健康检查

```bash
# 检查后端 API 是否正常运行
curl http://localhost:8080/api/dashboard/health

# 检查前端是否可访问
curl -I http://localhost:3000
```

## 四、生产环境优化配置

### 4.1 修改数据库密码（重要）

编辑 `docker-compose.yml`，修改 PostgreSQL 密码：

```yaml
services:
  postgres:
    environment:
      POSTGRES_PASSWORD: your_strong_password_here  # 修改此项
```

同时更新后端服务的数据库密码：

```yaml
  backend:
    environment:
      SPRING_DATASOURCE_PASSWORD: your_strong_password_here  # 保持一致
```

### 4.2 配置数据持久化

数据默认存储在 `./docker-data/postgres` 目录。建议挂载到独立的数据盘：

```yaml
services:
  postgres:
    volumes:
      - /data/postgres:/var/lib/postgresql/data  # 修改为实际数据目录
```

### 4.3 配置 Nginx 反向代理（推荐）

创建 Nginx 配置文件：

```bash
sudo nano /etc/nginx/sites-available/network-monitor
```

添加以下内容：

```nginx
server {
    listen 80;
    server_name your-domain.com;  # 替换为你的域名或服务器IP

    # 前端静态资源
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_cache_bypass $http_upgrade;
    }

    # 后端 API
    location /api/ {
        proxy_pass http://localhost:8080/api/;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_read_timeout 3600s;
    }

    # WebSocket
    location /ws/ {
        proxy_pass http://localhost:8080/ws/;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    }
}
```

启用配置：

```bash
# 创建软链接
sudo ln -s /etc/nginx/sites-available/network-monitor /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重载 Nginx
sudo systemctl reload nginx
```

### 4.4 配置 HTTPS（使用 Let's Encrypt）

```bash
# 安装 Certbot
sudo apt-get install -y certbot python3-certbot-nginx

# 申请证书
sudo certbot --nginx -d your-domain.com

# 自动续期测试
sudo certbot renew --dry-run
```

### 4.5 配置系统服务

创建 systemd 服务文件：

```bash
sudo nano /etc/systemd/system/network-monitor.service
```

添加内容：

```ini
[Unit]
Description=Network Monitor Application
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
RemainAfterExit=yes
WorkingDirectory=/opt/network-monitor
ExecStart=/usr/bin/docker compose up -d
ExecStop=/usr/bin/docker compose down
TimeoutStartSec=0

[Install]
WantedBy=multi-user.target
```

启用服务：

```bash
sudo systemctl daemon-reload
sudo systemctl enable network-monitor
sudo systemctl start network-monitor
```

## 五、日常运维命令

### 5.1 查看服务状态

```bash
# 查看容器状态
docker compose ps

# 查看资源使用
docker stats

# 查看实时日志
docker compose logs -f

# 查看最近100行日志
docker compose logs --tail=100
```

### 5.2 重启服务

```bash
# 重启所有服务
docker compose restart

# 重启单个服务
docker compose restart backend
docker compose restart frontend
docker compose restart postgres
```

### 5.3 更新部署

```bash
# 拉取最新代码
git pull

# 重新构建并启动
docker compose down
docker compose up -d --build

# 清理旧镜像
docker image prune -f
```

### 5.4 备份数据

```bash
# 创建备份目录
mkdir -p /backup/network-monitor

# 备份数据库
docker exec network-monitor-postgres pg_dump -U postgres network_monitor > /backup/network-monitor/db_$(date +%Y%m%d_%H%M%S).sql

# 备份配置文件
cp -r /opt/network-monitor /backup/network-monitor/config_$(date +%Y%m%d_%H%M%S)
```

### 5.5 恢复数据

```bash
# 恢复数据库
docker exec -i network-monitor-postgres psql -U postgres -d network_monitor < /backup/network-monitor/db_backup.sql
```

### 5.6 查看容器资源使用

```bash
# 查看容器资源占用
docker system df

# 清理未使用的资源
docker system prune -a --volumes
```

## 六、常见问题排查

### 6.1 容器无法启动

```bash
# 查看详细错误日志
docker compose logs <service-name>

# 检查端口占用
sudo netstat -tlnp | grep -E '3000|8080|5432'

# 释放被占用的端口
sudo kill -9 <PID>
```

### 6.2 数据库连接失败

```bash
# 检查数据库容器状态
docker compose ps postgres

# 进入数据库容器检查
docker exec -it network-monitor-postgres psql -U postgres -d network_monitor

# 检查网络连接
docker network ls
docker network inspect network-monitor-network
```

### 6.3 前端无法访问后端

```bash
# 检查后端服务状态
curl http://localhost:8080/api/dashboard/health

# 检查 Nginx 配置
sudo nginx -t

# 查看 Nginx 错误日志
sudo tail -f /var/log/nginx/error.log
```

### 6.4 内存不足

```bash
# 查看内存使用
free -h

# 添加 Swap 分区（如无 Swap）
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 永久生效
echo '/swapfile none swap sw 0 0' | sudo tee -a /etc/fstab
```

## 七、安全建议

1. **修改默认密码**：务必修改 PostgreSQL 的默认密码
2. **限制端口访问**：生产环境只开放 80/443 端口，关闭 3000/8080 的直接访问
3. **定期更新**：定期更新系统补丁和 Docker 镜像
4. **配置防火墙**：使用云服务商的安全组或服务器防火墙限制访问
5. **启用日志监控**：配置日志轮转和异常告警
6. **数据备份**：设置定时任务自动备份数据库

## 八、联系与支持

如有部署问题，请检查：
1. 各组件日志输出
2. 系统资源使用情况
3. 网络连通性

---

**文档版本**: 1.0  
**最后更新**: 2025-04-14  
**适用系统版本**: network-monitor v1.0.0
