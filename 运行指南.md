# 项目运行指南（Windows / PowerShell）

## 1. 环境要求
- JDK `17`
- Maven `3.9+`
- Node.js `18+`
- Docker Desktop（用于 PostgreSQL，可选）

默认端口：
- 前端：`http://localhost:3000`
- 后端：`http://localhost:8080`
- PostgreSQL：容器映射为 `5433`（容器内仍是 `5432`）

---

## 2. 本地开发启动（推荐）

### 2.1 启动 PostgreSQL（Docker）
```powershell
docker rm -f network-monitor-postgres-dev 2>$null
docker run -d --name network-monitor-postgres-dev `
  -e POSTGRES_USER=postgres `
  -e POSTGRES_PASSWORD=password `
  -e POSTGRES_DB=network_monitor `
  -p 5433:5432 `
  postgres:15-alpine
```

检查容器状态：
```powershell
docker ps --filter name=network-monitor-postgres-dev
```

### 2.2 启动后端
```powershell
cd D:\ASUS\Documents\jiedan\waibao\network-monitor-backend

$env:SPRING_DATASOURCE_URL='jdbc:postgresql://localhost:5433/network_monitor'
$env:SPRING_DATASOURCE_USERNAME='postgres'
$env:SPRING_DATASOURCE_PASSWORD='password'

mvn spring-boot:run
```

健康检查：
- `http://localhost:8080/api/dashboard/health`
- `http://localhost:8080/swagger-ui.html`

### 2.3 启动前端
```powershell
cd D:\ASUS\Documents\jiedan\waibao\network-monitor-frontend
npm install
npm run dev
```

访问：
- `http://localhost:3000`

---

## 3. Docker Compose 一键启动
```powershell
cd D:\ASUS\Documents\jiedan\waibao
docker compose up --build -d
docker compose logs -f
```

停止：
```powershell
docker compose down
```

---

## 4. ngrok 外网访问
确保前后端已本地启动后执行：
```powershell
ngrok http 3000
```

若遇到 Vite Host 限制，本项目已在 `network-monitor-frontend/vite.config.js` 放行常见 ngrok 域名后缀（`.ngrok-free.dev`、`.ngrok.app`）。

---

## 5. 演示数据自动维护（已内置）
- 启动时自动检测数据是否过期，过期则自动重建。
- 定时续流：默认每 `30s` 追加一批数据。
- 定时清理：按保留时长清理历史数据。
- 定时重建检查：默认每 `5min` 检查一次是否过期并自动重建。

关键参数（可用环境变量覆盖）：
- `APP_DEMO_STALE_MINUTES`（默认 `5`）
- `APP_DEMO_STREAM_INTERVAL_MS`（默认 `30000`）
- `APP_DEMO_STREAM_BATCH_FLOW_COUNT`（默认 `90`）
- `APP_DEMO_STREAM_WINDOW_SECONDS`（默认 `45`）
- `APP_DEMO_RETENTION_MINUTES`（默认 `120`）
- `APP_DEMO_REBUILD_CHECK_INTERVAL_MS`（默认 `300000`）

---

## 6. 常见问题排查

### 6.1 `dockerDesktopLinuxEngine` 管道报错
说明 Docker Desktop 没启动。先启动 Docker Desktop，再重试 `docker compose up`。

### 6.2 前端 `http proxy error` / `ECONNREFUSED`
说明后端未启动或端口不一致：
- 先访问 `http://localhost:8080/api/dashboard/health`
- 如后端改为其他端口，调整前端代理目标 `VITE_API_PROXY_TARGET`

### 6.3 Maven 启动失败（exit code 1）
执行以下命令看详细错误：
```powershell
mvn -e spring-boot:run
```
通常是数据库连接失败、端口冲突或 JDK 版本不匹配。

### 6.4 8080 端口被占用
```powershell
Get-NetTCPConnection -LocalPort 8080 -State Listen
Stop-Process -Id <PID>
```
