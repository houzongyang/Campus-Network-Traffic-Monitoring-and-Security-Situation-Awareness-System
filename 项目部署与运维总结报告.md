# 校园网络监控系统 - 项目部署与技术成果总结报告

## 1. 项目部署架构总结
针对 2GB 内存的极端环境，我们成功实现了一套**“异构跨云、轻量运行”**的部署架构：
- **监控中心 (主站)**: 部署于 Windows Server 2022 (8.146.228.64)，利用宝塔面板原生 Nginx + JDK 17 运行，最大限度节省了 Docker 带来的额外内存开销。
- **打流端 (模拟器)**: 部署于 Linux (60.205.56.61)，通过自动化脚本实现 PCAP 样例集的远程实时注入。
- **数据链路**: 打流端(Linux) -> REST API -> 后端分析(Java) -> 数据库(PostgreSQL) -> Nginx代理 -> 浏览器大屏。

## 2. 核心技术问题修复记录
在部署过程中，我们攻克了以下关键技术瓶颈：
- **跨平台构建兼容性**: 解决了 `package-lock.json` 在 Windows 与 Linux 容器间的不兼容问题，通过优化 Dockerfile 实现了依赖的动态生成。
- **WebSocket 实时通信**: 深度调优 Nginx 反向代理配置，通过显式配置 `Upgrade` 和 `Connection` 请求头，消除了大屏“重连中”的提示，实现了秒级数据推送。
- **内存溢出风险规避**: 通过 JVM 内存限制 (`-Xmx512m`) 和 Windows 虚拟内存调优，确保了系统在 2GB 内存环境下的长期稳定性。
- **时间平移算法应用**: 在 PCAP 导入时启用 `rebaseTimestamps` 逻辑，成功将 2019 年的历史样例集映射到当前时间，实现了“历史数据、实时演示”的效果。

## 3. 项目核心创新点 (算法价值)
系统不仅仅是一个监控工具，其内部集成了四项自主研发的算法：
- **EWMA-EAD**: 熵异常检测算法，有效识别隐蔽攻击。
- **MDTCE**: 威胁关联推理引擎，还原攻击全路径。
- **NBF-RS**: 行为指纹评分，为每个 IP 建立风险画像。
- **TPM-PA**: 时序预测预警，实现对流量异常的超前感知。

---

# 校园网络监控系统 - 演示环境运维手册 (打流专项)

## 1. 演示环境快速检查
- **前端访问**: `http://8.146.228.64:3000`
- **后端接口**: `http://8.146.228.64:8080/api/dashboard/overview`
- **监控状态**: 正常情况下应显示为 **“正常”** (绿色)，若显示“轮询”请检查 Nginx 代理。

## 2. 后台打流脚本维护 (Linux 60.205.56.61)
打流脚本 `play_traffic.sh` 负责持续向系统灌入 `sample.pcap` 的数据。

### 状态查看
```bash
# 查看打流日志（确认数据是否在持续推送）
tail -f /opt/network-monitor/play_traffic.log
```

### 启停操作
- **停止打流**: 
  ```bash
  ps -ef | grep play_traffic.sh | grep -v grep | awk '{print $2}' | xargs kill -9
  ```
- **启动打流 (后台静默模式)**:
  ```bash
  nohup ./play_traffic.sh > play_traffic.log 2>&1 &
  ```

## 3. 常见异常处理
- **大屏数据不跳动**: 
  1. 检查 Linux 端的打流脚本是否意外停止。
  2. 检查 Windows 端的 Java 后端窗口是否关闭或报错。
- **页面刷新 404**: 
  1. 确认宝塔 Nginx 配置文件中包含 `try_files $uri $uri/ /index.html;`。
- **内存告急**: 
  1. 在 Linux 脚本中增加 `sleep` 时间（如改至 60 秒）。
  2. 减小 `maxPackets` 参数（如改至 1000）。

---
**总结**: 系统目前已处于高度自动化的“运行即演示”状态，非常适合作为智慧校园网络安全治理的实战案例。
